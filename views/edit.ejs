<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10"> 
        <div class="flex justify-end">
            
            <a class="bg-red-500 rounded-md py-2 px-3 text-sm inline-block" href="/logout">Logout</a>
  
        </div>
         <h5 class="mb-5">Edit your post</h5>
        <form action="/update/<%=post._id%>" method="post">
             <textarea placeholder="Write whats on your mind?" class="block resize-none outline-none p-3 w-1/3 bg-transparent border-2 border-zinc-800 rounded-md" name="content"><%=post.content%></textarea>
             <input class="px-3 py-2 text-sm bg-yellow-500 text-black block rounded-md mt-2 w-40" type="submit" value="Update Post">

        </form>
       

       
    </div>
</body>
</html>