<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10"> 
        <div class="flex justify-end">
            
            <a class="bg-red-500 rounded-md py-2 px-3 text-sm inline-block" href="/logout">Logout</a>
  
        </div>
        <h3 class="text-3xl mb-5">Hello,<%=user.name%>👋👋</h3>
        <h5 class="mb-5">you can create a new post</h5>
        <form action="/post" method="post">
             <textarea placeholder="Write whats on your mind?" class="block resize-none outline-none p-3 w-1/3 bg-transparent border-2 border-zinc-800 rounded-md" name="content"></textarea>
             <input class="px-3 py-2 text-sm bg-blue-500 block rounded-md mt-2 w-40" type="submit" value="create New Post">

        </form>
      <div class="posts mt-20"> 
        <h3 class="text-zinc-400">Your Posts.</h3>
        <div class="postcontainer mt-5">
            <% user.posts.reverse().forEach(function(post){ %>
             <div class="post w-1/3 p-2 mb-4 rounded-md bg-zinc-700 border-[1px] border-2 border-zinc-800">
                <h4 class="text-blue-500 mb-2">@<%=user.username %></h4>
                <p class="text-sm tracking-tight"><%=post.content%></p>
                <div class="btns flex gap-4">
                    <a class="text-blue-500" href="/like/<%=post._id%>">Like</a>
                    <a class="text-zinc-600" href="">edit</a>
                </div>
             </div>
         <% }) %>
        </div>
      </div>

      </div>
    </div>
</body>
</html>